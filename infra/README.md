# infra — Infrastructure & Deployment Config

Placeholder. Will contain:
- Railway deployment config (backend)
- Vercel project config (admin dashboard)
- Docker Compose for local Postgres + PostGIS + Redis dev environment
- gitleaks config
